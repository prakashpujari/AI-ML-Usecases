FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY . /app

# Default command runs training; MLFLOW_TRACKING_URI can be provided at runtime
CMD ["python", "train.py", "--n-samples", "5000"]
